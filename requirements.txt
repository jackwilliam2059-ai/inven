# 재고관리 시스템 의존성
# pip install -r requirements.txt

# GUI
tkcalendar>=1.6.1
Pillow>=9.0.0

# 데이터베이스 (SQLAlchemy)
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.12.0

# Supabase (기존 호환용)
supabase>=2.0.0

# 엑셀 처리
openpyxl>=3.1.0
pandas>=2.0.0

# 환경 변수
python-dotenv>=1.0.0

# 웹 스크래핑 (이미지 검색)
requests>=2.28.0
beautifulsoup4>=4.12.0

# 기타
babel>=2.12.0
filelock>=3.12.0
